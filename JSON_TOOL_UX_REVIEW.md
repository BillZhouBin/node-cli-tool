# JSON 工具使用体验评估与优化方案

> 对象：`web/index.html` 内的 JSON 格式化/压缩工具（自研 per-line contenteditable 编辑器）
> 参考基准：JSON Editor Online、JSON Crack、jq Play、CodeMirror 6
> 日期：2026-09-10

---

## 0. 现状架构（先讲清楚问题根源）

当前编辑器不用现成内核，而是自己搭的：

```
#jsonEditor (.json-editor)
└── #jsonEditorLines (.json-editor-lines)
    └── .json-editor-line  × N           ← 每行一个 div
        ├── .json-editor-gutter          ← 行号 + 折叠箭头（user-select:none）
        └── .json-editor-content         ← contenteditable="true"（每行独立编辑宿主）
```

- 真源：`jsonLines[]`（每行文本数组）
- 结构：`foldPairs{}`（开行→闭行）、`collapsedFolds:Set`（已折叠开行）
- 渲染：`renderEditor()` **整块重建 innerHTML**
- 全屏：另建一份 `#fsEditorLines`（`fs-line-` 前缀）——**与主编辑器是两份独立 DOM**

**根源结论：两处设计决定了大部分不顺手——**

1. **每一行是独立的 `contenteditable`** → 原生跨行选区/复制/撤销都受限。
2. **全屏复制了第二份编辑器** → 状态（折叠/光标/滚动/撤销）来回同步必然丢失。

---

## 1. 当前工具的主要不顺手之处

### 1.1 复制 / 粘贴

| # | 问题 | 代码依据 | 影响 |
|---|------|---------|------|
| C1 | **只能整段复制**，选中局部再点「复制」仍复制全文 | `copyJsonContent()` → `getJsonText()` = `jsonLines.join('\n')`，完全不读选区 | 违背直觉，高频操作 |
| C2 | **没有「粘贴」按钮 / 没有剪贴板导入** | 只有 paste 事件处理，必须先把光标点进编辑区 | 空编辑器、移动端、不熟键盘的用户无法导入 |
| C3 | **多行粘贴不可靠** | paste 用 `document.execCommand('insertText', …)`（已废弃 API）插入到**单个** contenteditable 行 | 换行可能被吞成空格，或整段塞进一行 → `jsonLines` 行模型错乱、行号不刷新 |
| C4 | **没有导出** | 无 Blob/download 逻辑 | 无法保存 `.json` 文件 |
| C5 | **无法复制单个值 / 路径** | 无右键菜单、无「复制值」 | 取字段值必须手动拖选，易选错括号逗号 |
| C6 | **跨行选区行为不稳定** | 每行独立 contenteditable | 多行复制的新行分隔依赖浏览器行为，可能夹带多余换行 |
| C7 | **无「粘贴即格式化」** | 粘贴后不触发校验/美化 | 脏 JSON 粘进来还是原样，要再点一次美化 |

### 1.2 全屏模式功能不对等（与普通模式两套实现）

| # | 问题 | 代码依据 | 影响 |
|---|------|---------|------|
| F1 | **全屏只有「美化」一个按钮** | `toggleFullscreen()` 的 overlay.innerHTML 仅含美化 | 压缩/复制/清空/主题/粘贴/导出全缺 |
| F2 | **折叠状态丢失** | 退出全屏 `collapsedFolds.clear()` | 来回切换折叠全部被展开 |
| F3 | **错误提示降级为 `alert()`** | `processJsonFromFullscreen()` 用 alert 弹窗 | 与主模式内联红字不一致，阻塞 |
| F4 | **主题运行中不可切换** | 全屏内无主题按钮（只在进入时快照一次） | 需退出改主题再进 |
| F5 | **Esc 监听泄漏** | `addEventListener('keydown', escHandler, {once:true})` 每次进入都注册；用关闭按钮退出时该监听残留 | 下次按 Esc 会**意外重新打开全屏** |
| F6 | **光标/滚动不保留** | 进入后 `setTimeout(focus 第一行)`；退出后重渲染回顶 | 大文件切换割裂 |
| F7 | **两份 DOM 双写** | 主 `jsonLines` 与 `fsLines` 互相覆盖 | 易出现「这边改了那边没跟上」 |

### 1.3 编辑体验 / 校验

| # | 问题 | 代码依据 | 影响 |
|---|------|---------|------|
| E1 | **无撤销栈** | 每次 `renderEditor()`/`renderToContainer()` 重建 innerHTML | 美化、换行、删行后 Ctrl+Z 无效 |
| E2 | **折叠不实时** | 输入 `{`/`}` 不重算 `foldPairs`，箭头要等下次 render | 编辑与折叠状态错位 |
| E3 | **无实时校验** | 仅 `processJson()` 点击时校验 | 输入过程无 validity 反馈 |
| E4 | **无查找 / 替换** | 全局无 find 逻辑 | 大 JSON 定位字段只能肉眼扫 |
| E5 | **无全部折叠 / 展开** | 只能逐个点箭头 | 大 JSON 操作繁琐 |
| E6 | **无自动缩进 / 括号配对** | 回车、Tab 之外无处理 | 手写 JSON 体验差 |
| E7 | **无缩进宽度选择** | 固定 2 空格 | 与团队规范不符 |
| E8 | **无排序键 / 无 JSON Path** | — | 缺高级能力 |

### 1.4 视图形态

- 当前**只有纯文本行视图**，没有 Tree / Graph 视图。你说的「大图模式」目前并不存在——要做需要新增可视化层。

---

## 2. 主流工具的做法参考

| 工具 | 复制/粘贴 | 全屏/大图 | 校验与同步 | 值得抄的点 |
|------|-----------|-----------|-----------|-----------|
| **JSON Editor Online** | 选区天然复制；工具栏 Copy；右键 Copy value / Copy path | Text / Tree / Preview 三视图同屏切换，状态实时同步 | 实时解析，错误位置高亮 | 双栏同屏 + 树节点右键菜单 + Download |
| **JSON Crack** | 复制路径/值；导出 PNG/SVG | 节点图，缩放/拖拽/聚焦/折叠子树，大图懒加载 | 图↔文双向同步 | 大图交互范式（缩放/聚焦） |
| **jq Play** | 输入/输出各自独立，输出可复制 | 单页三段式 | 输入→查询→输出实时执行 | 实时反馈 + 错误即时显示 |
| **CodeMirror 6 / Monaco**（业界内核） | 原生多选、选区复制 | CSS 放大即可，无第二实例 | 支持 lint 实时校验 | 撤销栈/查找替换/括号配对/自动缩进/折叠 gutter 全部内建 |

**核心借鉴：** ① 编辑器**只有一个实例**（全屏是 CSS 放大，不是复制 DOM）；② 复制**尊重选区**；③ 校验**实时化**；④ 视图可切换但**状态单一真源**。

---

## 3. 优化方案（A / B / C 三档）

| 档位 | 做法 | 工作量 | 解决程度 | 建议 |
|------|------|--------|---------|------|
| **A. 轻量增强** | 保留自研编辑器，只补复制粘贴 + 全屏对齐 + 实时校验 | 小 | 解决你点名的两大痛点 | **先做** |
| **B. 换内核** | 引入 CodeMirror 6 作编辑内核（CDN 内联，保持单文件） | 中 | 撤销/选区/查找替换/折叠/校验几乎全免写 | **中期正解** |
| **C. 加视图** | 在 B 基础上加 Tree / Graph 视图，对齐 JSON Editor Online / JSON Crack | 大 | 完整对齐主流 | 按需 |

> 建议路径：**A 先止血 → B 做根治 → C 再扩形态**。

---

## 4. 具体功能优化点与预期交互

### 4.1 复制粘贴自由化（对应 C1–C7）

| 优化点 | 实现要点 | 预期交互 |
|--------|---------|---------|
| **智能复制按钮** | 有选区→复制选区；无选区→复制全文。`const text = window.getSelection().toString().trim() \|\| getJsonText()` | 选 3 行点「复制」→ 只复制这 3 行，toast「已复制 3 行 / 42 字符」 |
| **新增「粘贴」按钮** | `navigator.clipboard.readText()` → 走统一导入管线 | 空编辑器点「粘贴」→ 内容进来、自动校验、自动美化 |
| **统一导入管线 `importJson(text)`** | 按钮 / 编辑区粘贴 / 拖拽文件 / 拖入 URL 全走同一条：`trim → JSON.parse → 可选美化 → loadJsonText` | 无论从哪进，行为一致 |
| **修多行粘贴** | 弃用 `execCommand('insertText')`，改 `insertPastedText()`：按 `\n` 拆行、插入 `jsonLines`、重建索引再渲染 | 粘贴 50 行 JSON → 正确变成 50 行，行号/折叠刷新 |
| **右键菜单** | 自定义 `contextmenu`：复制值 / 复制路径 / 粘贴 / 美化 | 右键某字段 → 「复制路径」得到 `$.user.email` |
| **跨行复制修复** | 监听 `copy` 事件，跨 contenteditable 时主动按行拼装纯文本 | 跨行拖选复制 → 干净的多行文本，无 DOM 夹带 |
| **粘贴即格式化** | importJson 内置 beautify 开关 | 脏 JSON 粘进来立刻变整齐 |

### 4.2 导出（对应 C4）

| 优化点 | 预期交互 |
|--------|---------|
| **下载 JSON**：`Blob` + `a[download]`，文件名 `data-YYYYMMDD-HHmmss.json` | 点「下载」→ 浏览器直接存文件 |
| **复制为压缩 / 美化** 二选一 | 切换复制口径 |
| **复制转义 / 复制 JS 对象 / 复制 JSON Path** | 供后端/前端消费 |

### 4.3 全屏对齐（对应 F1–F7，最关键的一条）

> **改法：全屏不再复制编辑器，而是给 `#jsonEditor` 加 `.fullscreen` class（`position:fixed` 全屏）。DOM 不动。**

| 优化点 | 预期效果 |
|--------|---------|
| 单一编辑器实例放大 | 折叠、选区、光标、滚动、撤销栈**天然全部保留** |
| 全屏工具栏 = 主工具栏同一组 | 美化/压缩/复制/清空/主题/搜索/粘贴/导出 全都在 |
| 统一 keydown 管理 | Esc 退出后移除监听，杜绝「Esc 意外重开」 |
| 主题运行中可切 | 全屏内也能点 ☀️/🌙 |
| 错误内联 | 全屏不再 `alert()`，与主模式同款红字 |

### 4.4 校验与实时同步（对应 E2、E3）

| 优化点 | 预期交互 |
|--------|---------|
| **实时校验**：输入 300ms 防抖 → `JSON.parse` | 状态栏常显「✅ 有效 · 128 行 · 3.2 KB」或「❌ 第 12 行：Unexpected token }」 |
| **错误行高亮**：红色 gutter + 行背景，可点击定位 | 一眼看到错在哪行 |
| **折叠实时重算**：行内容变化后重算受影响的最小区间 | 输入 `{` 立刻出现折叠箭头 |
| **单一真源**：`jsonLines` 为唯一真源，contenteditable 只是渲染层 | 消除双写错位 |
| **宽松模式（可选）**：JSON5 / 去尾逗号 | 兼容手写配置 |

### 4.5 编辑体验（对应 E1、E4–E8）

| 优化点 | 预期交互 |
|--------|---------|
| **撤销/重做栈**：基于 `jsonLines` 快照，Ctrl+Z / Ctrl+Shift+Z | 美化后可撤销 |
| **查找替换（Ctrl+F）**：高亮全部匹配、上/下一个、替换/全部替换、结果计数 | 输入 `email` → 显示「1/7」，Enter 跳下一个 |
| **自动缩进**：回车继承缩进；`{`/`[` 后自动 +2；`}`/`]` 对齐 | 手写 JSON 不手动敲空格 |
| **括号/引号自动配对**，包裹选中文本 | 体验接近 IDE |
| **全部折叠 / 展开**（按层级） | 一键收拢大 JSON |
| **缩进宽度** 2 / 4 / Tab | 匹配团队规范 |
| **快捷键**：Cmd/Ctrl+Shift+F 美化、Cmd/Ctrl+S 下载 | 键盘流 |

### 4.6 大图 / Tree 视图（对应 §1.4，C 档）

| 优化点 | 预期交互 |
|--------|---------|
| 新增 Tree 视图（JSON Editor Online 式） | 可折叠节点、内联编辑、搜索定位、复制路径 |
| 新增 Graph 视图（JSON Crack 式） | 节点图，缩放/拖拽/聚焦/折叠子树，导出图片，大图懒加载 |
| 文本 ↔ 树/图 双向实时同步 | 单真源驱动多视图 |

---

## 5. 预期终态（一句话验收）

> 用户 `Cmd+V` 或点「粘贴」→ 脏 JSON 自动校验+美化，状态栏显示行数/体积；选 3 行点「复制」只复制 3 行；点「下载」得到 `.json`；点全屏**编辑器放大而非复制**，搜索/折叠/撤销/主题全在，退出后光标与折叠原样保留；Ctrl+F 弹出查找条并高亮定位。

---

## 6. 落地优先级（建议）

| 顺序 | 事项 | 档位 |
|------|------|------|
| P0 | 智能复制（尊重选区）+ 粘贴按钮 + 修多行粘贴 | A |
| P0 | 全屏改 CSS 放大单实例，工具栏对齐，修 Esc 泄漏 | A |
| P1 | 实时校验 + 状态栏 + 错误行高亮 | A |
| P1 | 下载导出 | A |
| P2 | 撤销栈 / 自动缩进 / 查找替换 | A→B |
| P2 | 引入 CodeMirror 6 内核 | B |
| P3 | Tree / Graph 视图 | C |
